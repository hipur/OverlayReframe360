# <Task name> — findings

What we learn while working: decisions made, dead-ends, and **code locations** (so the next agent
doesn't re-search). Keep it terse — `file:line — what's there`.

## Code locations

- `path/to/file.ts:NN` — <what lives here / why it matters>

## Decisions

- <decision> — <why> (link the spec if it changed).

## Dead-ends / gotchas

- <thing that didn't work> — <why, so nobody retries it>.
